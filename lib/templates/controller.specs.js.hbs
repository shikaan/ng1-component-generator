describe("Controller: {{ModuleName}}{{controller}}Controller", function() {
  let scope,
  {{ModuleName}}{{controller}}Controller,
  serviceMock;

  beforeEach(module("{{module-name}}"));

  beforeEach(inject(
    function ($controller, $rootScope) {
      scope = $rootScope.$new();

      {{ModuleName}}{{controller}}Controller = $controller("{{ModuleName}}{{controller}}Controller");

      scope.$apply();
    }
  ));
});